/// Module which contains trait for `Context` and enum for simulation of polymorphism.
pub mod context;
/// Init Context
pub mod init_context;
/// Query Context
pub mod query_context;
/// Transactional Context
pub mod tx_context;
